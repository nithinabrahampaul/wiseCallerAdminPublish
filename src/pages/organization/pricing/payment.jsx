import React from "react";
import { Card } from "react-bootstrap";

export const Payment = ({
  onPageChange,
  activeStep,
  pricingForm,
  onFormChange,
}) => {
  return (
    <React.Fragment>
      <Card className="mb-4 mb-xl-0">
        <Card.Header className="border-gray-100 py-5 px-4"></Card.Header>
      </Card>
    </React.Fragment>
  );
};
